<local:ContentPage
    x:Class="PrivateWiki.Pages.ContentPages.HtmlPageViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrivateWiki.Pages.ContentPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:privateWiki="using:PrivateWiki"
    xmlns:controls="using:PrivateWiki.Controls"
    xmlns:controls1="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls2="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid
        x:Name="PageViewerGrid"
        Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Popup
            Name="SearchPopup"
            HorizontalAlignment="Center"
            IsLightDismissEnabled="True"
            LightDismissOverlayMode="On">

            <privateWiki:SearchPopupContent x:Name="SearchPopupContentName" />

        </Popup>

        <controls:PageViewerCommandBar
            Name="commandBar"
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Height="Auto"
            Background="{ThemeResource SystemControlBackgroundAccentBrush}"
            SettingsClick="Setting_Click"
            TopClick="Top_Click"
            PdfClick="Pdf_Click"
            EditClick="Edit_Click"
            SearchClick="Search_Click"
            HistoryClick="Revision_Click"
            FullscreenClick="Fullscreen_Click"
            ExportClick="Export_Click"
            ImportClick="Import_Click"
            NavigateToPage="CommandBar_OnNavigateToPage"/>

        <Border
            Grid.Row="1"
            Grid.Column="0"
            BorderBrush="Gray"
            BorderThickness="0"
            CornerRadius="4"
            Padding="5, 0, 5, 5">
            <WebView
                Name="Webview"
                MinHeight="100"
                MinWidth="100"
                Width="Auto"
                Height="Auto"
                DefaultBackgroundColor="White"/>
        </Border>

        <!--

        <controls1:GridSplitter
            Grid.Row="1"
            Grid.Column="1"
            Width="5"
            MinWidth="0"
            ResizeBehavior="PreviousAndNext"
            ResizeDirection="Columns"
            Background="#EEEEEE"
            Foreground="#EEEEEE"
            FontSize="13"
            Padding="0, 0, 0, 0" />


        <StackPanel
            Name="RightMenu"
            Grid.Row="1"
            Grid.Column="2"
            Padding="0, 10, 10, 20"
            Margin="0,0,0,0"
            Background="#EEEEEE">

            <TextBlock
                x:Uid="/PageViewer/TOC"
                Name="TOCHeader"
                HorizontalAlignment="Left"
                FontSize="16"
                TextDecorations="Underline"
                Padding="10, 0, 0, 20" />

            <controls2:TreeView
                Name="Treeview"
                ItemInvoked="TreeView_ItemInvoked"
                MinWidth="250"
                VerticalAlignment="Top" />

            <TextBlock
                x:Uid="/PageViewer/Tags"
                Name="TagsHeader"
                HorizontalAlignment="Left"
                FontSize="16"
                TextDecorations="Underline"
                Padding="10, 0, 0, 20" />

            <controls1:WrapPanel
                x:Name="TagsPanel"/>

        </StackPanel>

        -->

    </Grid>
</local:ContentPage>
